import { CalendlyDirective } from './calendly.directive';

describe('CalendlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendlyDirective();
    expect(directive).toBeTruthy();
  });
});
